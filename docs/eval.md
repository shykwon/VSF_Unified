🔴 실행 차단 문제 (즉시 수정 필요)

  1. UnifiedTrainer.fit() 메서드 없음

  # scripts/train.py:101 에서 호출하지만
  trainer.fit(train_loader, val_loader, epochs=args.epochs)

  # src/core/trainer.py 에는 fit() 메서드가 없음!
  # → AttributeError 발생

  2. SAITS 마스크 의미 반전

  # src/models/saits/wrapper.py:80
  inputs = {
      "missing_mask": mask,  # ← 문제!
  }
  # Dataset: mask=1 → 관측됨
  # SAITS 기대: missing_mask=1 → 결측
  # → 관측값을 결측으로 처리!

  3. 재현성 없음

  # 전체 코드베이스에서 시드 설정 없음
  # np.random.seed() ❌
  # torch.manual_seed() ❌
  # → 매 실행마다 다른 결과

  ---
  🟠 공정 비교 불가 문제

  4. 손실 함수 불일치
  ┌───────┬────────────────┬───────────┐
  │ 모델  │   원본 논문    │ 현재 구현 │
  ├───────┼────────────────┼───────────┤
  │ FDW   │ Masked MAE     │ MSE ❌    │
  ├───────┼────────────────┼───────────┤
  │ GinAR │ Masked MAE     │ MSE ❌    │
  ├───────┼────────────────┼───────────┤
  │ CSDI  │ Diffusion Loss │ ✅ 내부   │
  ├───────┼────────────────┼───────────┤
  │ SRDI  │ Diffusion Loss │ ✅ 내부   │
  ├───────┼────────────────┼───────────┤
  │ SAITS │ Reconstruction │ ✅ 내부   │
  └───────┴────────────────┴───────────┘
  5. 출력 Shape 불일치

  FDW:    (B, T, N, C) → ✅ 정상 (permute 적용됨)
  GinAR:  (B, T, N)    → ⚠️ C 차원 없음
  CSDI:   (B, T, N, 1) → ✅ 정상
  SAITS:  (B, T, N, 1) → ✅ 정상
  GIMCC:  None         → ❌ 미구현

  6. 정규화 전략 없음

  # src/data/loader.py:46-61
  # Train/Val/Test 분할 후 정규화 없음
  # → 데이터 누수 가능성

  ---
  🟢 잘 구현된 부분

  평가 메트릭 (src/core/metrics.py)

  class Metrics:
      MAE, MSE, RMSE, MAPE  # ✅ 표준 메트릭

  class MaskedMetrics:
      MaskedMAE, MaskedMSE, MaskedRMSE  # ✅ 마스크 메트릭

  데이터 로더 (src/data/loader.py)

  class WindowedVSFDataset:  # ✅ 잘 구현됨
      # x: (T_in, N, C)
      # y: (T_out, N, C)
      # mask: (T_in, N, C)
